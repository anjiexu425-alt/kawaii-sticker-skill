# 主题模式输入示例：一个主题词 + 1 张参考图

<!-- input: 打工人 -->
<!-- reference: 1, text-described (详见正文描述，不嵌入图片) -->

> 本文件是**机器可读的输入声明**：`<!-- input: ... -->` 声明主题词；`<!-- reference: ... -->` 声明参考图数量与形态（约定见 `examples/README.md` §3）。主题模式下输出契约的 6 句文案由**助手自拟**（不要求与主题词相等，但必须两两不同），校验器不比对主题词与 `text.content` 的相等性。

## 用户请求 (User Request)

> 来一套「**打工人**」主题贴纸，用我这张参考图的角色。

用户给了一个**名词性主题词**，并上传了 **1 张参考图**（本示例以**文字描述**代替真实图片——仓库不嵌入任何图片素材）。

## 参考图描述（文字形式，非真实图片）

> 一张**手绘风格的角色半身图**：奶油白、圆润的兔头，一对**长垂耳兔耳**、内耳蜜桃粉；**大眼高光、圆溜溜**；小圆鼻、w 嘴；头顶别着**珍珠发夹**，胸前挂着**小工牌**（写着"上班中"）；Q 版 2 头身；铅笔线稿 + 粉彩平涂。

## 运行前解析（本示例的判定过程）

1. **模式判定 → 主题模式（theme）**：输入是名词性主题「打工人」且非口语话术 → SPEC §3.1 优先级 3。
2. **撰写文案（助手自拟）**：围绕「打工人」自拟 **6 句互不相同**的短文案（中文、口语化、有梗、可单句成贴纸，SPEC §3.3-2）——见 `output-6-pack.md` 的 6 个 `text.content`。
3. **角色解析 → 有参考图 → 特征图管线**：按 SPEC §4.1 提取 `feature_map`，`character.source = "image"`，**锁定**：整套 6 张共享同一份 `feature_map`。
4. **能力协商 → 路径 ②（契约输出）**：假设宿主无原生生图工具 → 输出一个 `kss-prompt` 块（见 `output-6-pack.md`），块内为 6 个契约对象的数组。

## 校验要点 (Validator Notes)

- [ ] 机器标记 `<!-- input: 打工人 -->` 与 `<!-- reference: 1, ... -->` 存在且可解析。
- [ ] 对应输出 `output-6-pack.md` 的 `kss-prompt` 块解析为**长度恰为 6 的数组**；`mode` 全部为 `"theme"`。
- [ ] 6 个 `text.content` **两两不同**；6 个对象的 `expression`/`pose_action`/`composition` 组合**两两不同**。
- [ ] 6 个对象 `style_anchor`、`character.feature_map`、`output.*` **完全相同**（整套一致性）；`character.source` 全部为 `"image"`。
- [ ] 主题模式不要求文案与主题词相等（校验器不得用「打工人」逐字比对 `text.content`）。
